<UserControl x:Class="Cmux.Controls.WorkspaceSidebarItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:Cmux.ViewModels"
             Cursor="Hand"
             MinHeight="32">

    <Grid>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Rename" InputGestureText="F2" Click="Rename_Click">
                    <MenuItem.Icon>
                        <ui:FontIcon Glyph="&#xE8AC;" FontSize="12" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Duplicate" Click="Duplicate_Click">
                    <MenuItem.Icon>
                        <ui:FontIcon Glyph="&#xE8C8;" FontSize="12" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Set Workspace Icon" Click="SetIcon_Click">
                    <MenuItem.Icon>
                        <ui:FontIcon Glyph="&#xE8A5;" FontSize="12" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Accent">
                    <MenuItem.Icon>
                        <TextBlock Text="●" Foreground="{Binding AccentColor, Converter={StaticResource HexToBrush}}" />
                    </MenuItem.Icon>
                    <MenuItem Header="Indigo" Click="SetColor_Click" Tag="#FF818CF8">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FF818CF8" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Green" Click="SetColor_Click" Tag="#FF10B981">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FF10B981" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Amber" Click="SetColor_Click" Tag="#FFF59E0B">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FFF59E0B" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Red" Click="SetColor_Click" Tag="#FFEF4444">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FFEF4444" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Cyan" Click="SetColor_Click" Tag="#FF06B6D4">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FF06B6D4" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Purple" Click="SetColor_Click" Tag="#FFA855F7">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FFA855F7" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Slate" Click="SetColor_Click" Tag="#FF64748B">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FF64748B" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Pink" Click="SetColor_Click" Tag="#FFEC4899">
                        <MenuItem.Icon>
                            <TextBlock Text="●" Foreground="#FFEC4899" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Custom..." Click="SetCustomColor_Click">
                        <MenuItem.Icon>
                            <TextBlock Text="＋" Foreground="{StaticResource ForegroundBrush}" FontSize="12" FontWeight="Bold" />
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
                <Separator />
                <MenuItem Click="TogglePin_Click">
                    <MenuItem.Style>
                        <Style TargetType="MenuItem">
                            <Setter Property="Header" Value="Pin Workspace" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                    <Setter Property="Header" Value="Unpin Workspace" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                    <MenuItem.Icon>
                        <ui:FontIcon FontSize="12">
                            <ui:FontIcon.Style>
                                <Style TargetType="ui:FontIcon">
                                    <!-- Pin glyph (E718) when unpinned, Unpin glyph (E77A) when pinned -->
                                    <Setter Property="Glyph" Value="&#xE718;" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                            <Setter Property="Glyph" Value="&#xE77A;" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:FontIcon.Style>
                        </ui:FontIcon>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Header="New Surface" InputGestureText="Ctrl+T" Click="NewSurface_Click">
                    <MenuItem.Icon>
                        <ui:FontIcon Glyph="&#xE710;" FontSize="12" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Header="Move Up" Click="MoveUp_Click">
                    <MenuItem.Icon>
                        <ui:FontIcon Glyph="&#xE74A;" FontSize="12" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Move Down" Click="MoveDown_Click">
                    <MenuItem.Icon>
                        <ui:FontIcon Glyph="&#xE74B;" FontSize="12" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Header="Close Workspace" InputGestureText="Ctrl+Shift+W" Click="Close_Click"
                          Foreground="#FFEF4444">
                    <MenuItem.Icon>
                        <ui:FontIcon Glyph="&#xE711;" FontSize="12"
                                     Foreground="#FFEF4444" />
                    </MenuItem.Icon>
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <!-- Workspace name + notification badge -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="16" Height="16" CornerRadius="4"
                        Background="{Binding AccentColor, Converter={StaticResource HexToBrush}}"
                        VerticalAlignment="Center" Margin="0,0,6,0">
                    <TextBlock Text="{Binding IconGlyph}" FontFamily="{Binding IconFontFamily}" FontSize="10"
                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <Grid Grid.Column="1" x:Name="NameHost">
                    <TextBlock x:Name="NameDisplay" Text="{Binding Name}"
                               Foreground="{StaticResource ForegroundBrush}"
                               FontSize="13" FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"
                               MouseLeftButtonDown="NameDisplay_MouseLeftButtonDown" />
                    <TextBox x:Name="NameEditor" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Foreground="{StaticResource ForegroundBrush}"
                             Background="{StaticResource SidebarItemHoverBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1"
                             FontSize="13" FontWeight="SemiBold" Padding="2,0"
                             Visibility="Collapsed"
                             LostFocus="NameEditor_LostFocus"
                             KeyDown="NameEditor_KeyDown" />
                </Grid>
            </Grid>

            <!-- Pin indicator + notification badge -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:FontIcon Glyph="&#xE840;" FontSize="11"
                             Foreground="{StaticResource ForegroundDimBrush}"
                             VerticalAlignment="Center" Margin="4,0,0,0"
                             ToolTip="Pinned"
                             Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibility}}" />
                <Border CornerRadius="8"
                        Background="{StaticResource NotificationBrush}"
                        Padding="5,1" Margin="4,0,0,0"
                        Visibility="{Binding HasNotification, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="{Binding UnreadNotificationCount}"
                               Foreground="White" FontSize="10" />
                </Border>
            </StackPanel>
        </Grid>

        <!-- Git branch -->
        <StackPanel x:Name="GitInfo" Grid.Row="1" Orientation="Horizontal" Margin="0,3,0,0"
                    Visibility="{Binding GitBranch, Converter={StaticResource NullToCollapsed}}">
            <ui:FontIcon Glyph="&#xE8AD;" FontSize="10"
                         Foreground="{StaticResource ForegroundDimBrush}" Margin="0,0,4,0"
                         VerticalAlignment="Center" />
            <TextBlock Text="{Binding GitBranch}"
                       Foreground="{StaticResource AccentBrush}"
                       FontSize="11" TextTrimming="CharacterEllipsis" />
        </StackPanel>

        <!-- Working directory -->
        <TextBlock x:Name="WorkingDirInfo" Grid.Row="2" Text="{Binding WorkingDirectory}"
                   Foreground="{StaticResource ForegroundDimBrush}"
                   FontSize="10" Margin="0,2,0,0"
                   TextTrimming="CharacterEllipsis"
                   Visibility="{Binding WorkingDirectory, Converter={StaticResource NullToCollapsed}}" />

        <!-- Latest notification text -->
        <TextBlock x:Name="LatestNotificationInfo" Grid.Row="3" Text="{Binding LatestNotificationText}"
                   Foreground="{StaticResource ForegroundDimBrush}"
                   FontSize="10" Margin="0,2,0,0"
                   TextTrimming="CharacterEllipsis" FontStyle="Italic"
                   Visibility="{Binding LatestNotificationText, Converter={StaticResource NullToCollapsed}}" />
    </Grid>
</UserControl>
