<UserControl x:Class="Cmux.Controls.NotificationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="350" Background="#FF202020">

    <Border CornerRadius="8,0,0,8" BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1,0,0,0" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Background="#FF252525" Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Notifications" FontSize="14"
                           FontWeight="SemiBold" Foreground="{StaticResource ForegroundBrush}"
                           Margin="12,8" VerticalAlignment="Center" />

                <Button Grid.Column="1" Content="Mark all read"
                        Style="{StaticResource CmuxButton}"
                        FontSize="11" Margin="0,4,4,4"
                        Command="{Binding MarkAllNotificationsReadCommand}" />

                <Button Grid.Column="2" Content="&#xE10A;" FontFamily="Segoe MDL2 Assets"
                        Style="{StaticResource CmuxButton}"
                        FontSize="10" Margin="0,4,8,4" Padding="6,4"
                        Command="{Binding ToggleNotificationPanelCommand}" />
            </Grid>

            <!-- Notification list -->
            <ListBox Grid.Row="1"
                     ItemsSource="{Binding NotificationService.Notifications}"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12,8" BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Title + unread dot -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal">
                                    <Ellipse Width="6" Height="6" Margin="0,0,6,0"
                                             Fill="{StaticResource NotificationBrush}"
                                             VerticalAlignment="Center">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRead}" Value="False">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="{StaticResource ForegroundBrush}"
                                               FontSize="12" FontWeight="SemiBold" />
                                </StackPanel>

                                <!-- Body -->
                                <TextBlock Grid.Row="1" Text="{Binding Body}"
                                           Foreground="{StaticResource ForegroundDimBrush}"
                                           FontSize="11" Margin="0,2,0,0"
                                           TextWrapping="Wrap" MaxHeight="40"
                                           TextTrimming="CharacterEllipsis" />

                                <!-- Timestamp -->
                                <TextBlock Grid.Row="2" Text="{Binding Timestamp, StringFormat=HH:mm:ss}"
                                           Foreground="{StaticResource ForegroundDimBrush}"
                                           FontSize="10" Margin="0,4,0,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Empty state -->
            <TextBlock Grid.Row="1" Text="No notifications"
                       Foreground="{StaticResource ForegroundDimBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="13">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding NotificationService.Notifications.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
