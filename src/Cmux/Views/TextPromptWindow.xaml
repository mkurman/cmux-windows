<Window x:Class="Cmux.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="190" Width="460"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText"
                   Grid.Row="0"
                   Foreground="{StaticResource ForegroundBrush}"
                   TextWrapping="Wrap"
                   FontSize="13" />

        <TextBox x:Name="InputTextBox"
                 Grid.Row="1"
                 Margin="0,10,0,0"
                 Padding="8,5"
                 Background="#FF1A1A1A"
                 Foreground="{StaticResource ForegroundBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 BorderThickness="1"
                 CaretBrush="{StaticResource ForegroundBrush}"
                 SelectionBrush="{StaticResource AccentBrush}"
                 KeyDown="InputTextBox_KeyDown" />

        <TextBlock Grid.Row="2"
                   Margin="0,8,0,0"
                   Foreground="{StaticResource ForegroundDimBrush}"
                   FontSize="11"
                   Text="Press Enter to confirm or Esc to cancel." />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Style="{StaticResource CmuxButton}" Padding="12,6" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="OK" Style="{StaticResource AccentButton}" Padding="14,6" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
