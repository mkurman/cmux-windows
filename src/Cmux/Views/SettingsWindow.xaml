<Window x:Class="Cmux.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="Settings" Height="600" Width="700"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Force dark system brush overrides inside settings window -->
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF1A1A1A" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF1A1A1A" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FFE2E2E9" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFE2E2E9" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF252547" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFE2E2E9" />

        <!-- Sidebar nav-button style — local because it's nav-specific. -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundDimBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="5" Margin="4,1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#20FFFFFF" />
                            </Trigger>
                            <Trigger Property="Tag" Value="active">
                                <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
                                <Setter TargetName="Bd" Property="Background" Value="#30FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}" CornerRadius="10" BorderThickness="1"
            BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.6" Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Settings" FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource ForegroundBrush}"
                               VerticalAlignment="Center" Margin="18,0,0,0" />
                    <Button x:Name="CloseBtn" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Margin="0,0,10,0" Click="CloseButton_Click"
                            Style="{StaticResource IconButton}" Content="&#xE8BB;"
                            FontFamily="Segoe MDL2 Assets" FontSize="12" />
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left Nav Sidebar -->
                <StackPanel Grid.Column="0" Margin="0,8,0,0">
                    <Button x:Name="NavAppearance" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="Appearance" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavTerminal" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE756;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="Terminal" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavBehavior" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="Behavior" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavKeyboard" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE765;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="Keyboard" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavAbout" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="About" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Divider -->
                <Border Grid.Column="1" Background="{StaticResource BorderBrush}" />

                <!-- Content Area -->
                <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Padding="24,16">
                    <Grid>

                    <!-- ═══ Appearance Section ═══ -->
                    <StackPanel x:Name="AppearanceSection">
                        <TextBlock Text="Appearance" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- Font Family -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Font Family" Style="{StaticResource CmuxFormLabel}" />
                            <ComboBox x:Name="FontFamilyCombo" Grid.Column="1"
                                      Style="{StaticResource CmuxComboBox}"
                                      Width="320" HorizontalAlignment="Left"
                                      IsTextSearchEnabled="True" />
                        </Grid>

                        <!-- Font Size -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Font Size" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Slider x:Name="FontSizeSlider" Minimum="9" Maximum="28"
                                        Width="220" VerticalAlignment="Center"
                                        TickFrequency="1" IsSnapToTickEnabled="True"
                                        ValueChanged="FontSizeSlider_ValueChanged" />
                                <TextBlock x:Name="FontSizeValueText" Foreground="{StaticResource ForegroundDimBrush}"
                                           VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12" />
                            </StackPanel>
                        </Grid>

                        <!-- Theme -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Theme" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <ComboBox x:Name="ThemeCombo" Style="{StaticResource CmuxComboBox}"
                                              Width="200" SelectionChanged="ThemeCombo_SelectionChanged" />
                                <Border x:Name="ThemePreview" Width="64" Height="{StaticResource ControlHeight}" Margin="12,0,0,0"
                                        CornerRadius="{StaticResource ControlCornerRadius}" BorderThickness="1"
                                        BorderBrush="{StaticResource BorderBrush}">
                                    <TextBlock x:Name="ThemePreviewText" Text="Aa" FontSize="13"
                                               FontWeight="SemiBold" HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>
                            </StackPanel>
                        </Grid>

                        <!-- Opacity -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Opacity" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Slider x:Name="OpacitySlider" Minimum="0.5" Maximum="1.0"
                                        Width="200" VerticalAlignment="Center"
                                        TickFrequency="0.01" IsSnapToTickEnabled="True"
                                        ValueChanged="OpacitySlider_ValueChanged" />
                                <TextBlock x:Name="OpacityValueText" Foreground="{StaticResource ForegroundDimBrush}"
                                           VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12" />
                            </StackPanel>
                        </Grid>

                        <!-- Cursor Style -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Cursor Style" Style="{StaticResource CmuxFormLabel}" />
                            <ComboBox x:Name="CursorStyleCombo" Grid.Column="1"
                                      Style="{StaticResource CmuxComboBox}"
                                      Width="140" HorizontalAlignment="Left" />
                        </Grid>

                        <!-- Cursor Blink -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Cursor Blink" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="CursorBlinkCheck" Grid.Column="1"
                                             OffContent="" OnContent="" MinWidth="0" />
                        </Grid>

                        <!-- System Theme (detected) -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="System Theme" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="SystemThemeText" Text="Dark" FontSize="13"
                                           Foreground="{StaticResource ForegroundBrush}" />
                                <TextBlock Text=" (detected)" FontSize="12" Foreground="#FF6B7280" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <!-- ═══ Terminal Section ═══ -->
                    <StackPanel x:Name="TerminalSection" Visibility="Collapsed">
                        <TextBlock Text="Terminal" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- Color Preset -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Color Preset" Style="{StaticResource CmuxFormLabel}" />
                            <ComboBox x:Name="TerminalThemePresetCombo" Grid.Column="1"
                                      Style="{StaticResource CmuxComboBox}"
                                      Width="240" HorizontalAlignment="Left"
                                      SelectionChanged="TerminalThemePresetCombo_SelectionChanged" />
                        </Grid>

                        <!-- Use custom colors -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Custom Colors" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="UseCustomTerminalColorsCheck" Grid.Column="1"
                                             OffContent="Override preset colors"
                                             OnContent="Override preset colors"
                                             Toggled="UseCustomTerminalColorsCheck_Changed" />
                        </Grid>

                        <!-- Terminal color swatches -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Background" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalBackgroundColorPanel" VerticalAlignment="Center">
                                <Border x:Name="TerminalBackgroundPreview" Style="{StaticResource CmuxColorSwatch}" Margin="0,0,10,0" />
                                <TextBox x:Name="TerminalBackgroundHexBox" Style="{StaticResource CmuxTextBox}" Width="120" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="Pick" Style="{StaticResource CmuxButton}" Padding="14,7"
                                        Click="PickTerminalBackgroundColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Foreground" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalForegroundColorPanel" VerticalAlignment="Center">
                                <Border x:Name="TerminalForegroundPreview" Style="{StaticResource CmuxColorSwatch}" Margin="0,0,10,0" />
                                <TextBox x:Name="TerminalForegroundHexBox" Style="{StaticResource CmuxTextBox}" Width="120" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="Pick" Style="{StaticResource CmuxButton}" Padding="14,7"
                                        Click="PickTerminalForegroundColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Cursor" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalCursorColorPanel" VerticalAlignment="Center">
                                <Border x:Name="TerminalCursorPreview" Style="{StaticResource CmuxColorSwatch}" Margin="0,0,10,0" />
                                <TextBox x:Name="TerminalCursorHexBox" Style="{StaticResource CmuxTextBox}" Width="120" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="Pick" Style="{StaticResource CmuxButton}" Padding="14,7"
                                        Click="PickTerminalCursorColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Selection" Style="{StaticResource CmuxFormLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalSelectionColorPanel" VerticalAlignment="Center">
                                <Border x:Name="TerminalSelectionPreview" Style="{StaticResource CmuxColorSwatch}" Margin="0,0,10,0" />
                                <TextBox x:Name="TerminalSelectionHexBox" Style="{StaticResource CmuxTextBox}" Width="120" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="Pick" Style="{StaticResource CmuxButton}" Padding="14,7"
                                        Click="PickTerminalSelectionColor_Click" />
                                <Button Content="Reset" Style="{StaticResource CmuxButton}" Padding="14,7" Margin="8,0,0,0"
                                        Click="ResetTerminalColors_Click" />
                            </StackPanel>
                        </Grid>

                        <!-- Default Shell -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Default Shell" Style="{StaticResource CmuxFormLabel}" />
                            <ComboBox x:Name="ShellCombo" Grid.Column="1"
                                      Style="{StaticResource CmuxComboBox}"
                                      Background="#FF1A1A1A" Foreground="{StaticResource ForegroundBrush}" />
                        </Grid>

                        <!-- Shell Arguments -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Shell Arguments" Style="{StaticResource CmuxFormLabel}" />
                            <TextBox x:Name="ShellArgsBox" Grid.Column="1"
                                     Style="{StaticResource CmuxTextBox}" />
                        </Grid>

                        <!-- Scrollback Lines -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Scrollback Lines" Style="{StaticResource CmuxFormLabel}" />
                            <ui:NumberBox x:Name="ScrollbackBox" Grid.Column="1"
                                          Width="140" HorizontalAlignment="Left"
                                          Minimum="0" Maximum="1000000" SmallChange="100" LargeChange="1000"
                                          SpinButtonPlacementMode="Compact" />
                        </Grid>

                        <!-- Visual Bell -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Visual Bell" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="VisualBellCheck" Grid.Column="1"
                                             OffContent="" OnContent="" MinWidth="0" />
                        </Grid>

                        <!-- Bracketed Paste -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Bracketed Paste" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="BracketedPasteCheck" Grid.Column="1"
                                             OffContent="" OnContent="" MinWidth="0" />
                        </Grid>
                    </StackPanel>

                    <!-- ═══ Behavior Section ═══ -->
                    <StackPanel x:Name="BehaviorSection" Visibility="Collapsed">
                        <TextBlock Text="Behavior" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- Restore Session -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Restore Session" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="RestoreSessionCheck" Grid.Column="1"
                                             OffContent="Restore previous session on startup"
                                             OnContent="Restore previous session on startup" />
                        </Grid>

                        <!-- Confirm on Close -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Confirm Close" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="ConfirmCloseCheck" Grid.Column="1"
                                             OffContent="Ask before closing window"
                                             OnContent="Ask before closing window" />
                        </Grid>

                        <!-- Auto Copy -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Auto Copy" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="AutoCopyCheck" Grid.Column="1"
                                             OffContent="Copy to clipboard on text selection"
                                             OnContent="Copy to clipboard on text selection" />
                        </Grid>

                        <!-- Right-Click Paste -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Right-Click Paste" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="RightClickPasteCheck" Grid.Column="1"
                                             OffContent="Paste clipboard on right-click (skips context menu)"
                                             OnContent="Paste clipboard on right-click (skips context menu)" />
                        </Grid>

                        <!-- Ctrl+Click URLs -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Ctrl+Click URLs" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="CtrlClickUrlCheck" Grid.Column="1"
                                             OffContent="Open URLs with Ctrl+Click"
                                             OnContent="Open URLs with Ctrl+Click" />
                        </Grid>

                        <!-- Toast Notifications -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Toast Notifications" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="ToastEnabledCheck" Grid.Column="1"
                                             OffContent="Show Windows toasts for OSC 9 / cmux notify events"
                                             OnContent="Show Windows toasts for OSC 9 / cmux notify events" />
                        </Grid>

                        <!-- Toasts While Focused -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Toasts While Focused" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="ToastWhileFocusedCheck" Grid.Column="1"
                                             OffContent="Show toasts even when cmux window is in front"
                                             OnContent="Show toasts even when cmux window is in front" />
                        </Grid>

                        <!-- Auto Save Interval -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Auto Save (sec)" Style="{StaticResource CmuxFormLabel}" />
                            <ui:NumberBox x:Name="AutoSaveBox" Grid.Column="1"
                                          Width="120" HorizontalAlignment="Left"
                                          Minimum="0" Maximum="3600" SmallChange="5" LargeChange="30"
                                          SpinButtonPlacementMode="Compact" />
                        </Grid>

                        <!-- Log Retention -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Log Retention (days)" Style="{StaticResource CmuxFormLabel}" />
                            <ui:NumberBox x:Name="LogRetentionDaysBox" Grid.Column="1"
                                          Width="120" HorizontalAlignment="Left"
                                          Minimum="0" Maximum="3650" SmallChange="1" LargeChange="30"
                                          SpinButtonPlacementMode="Compact"
                                          ToolTip="Delete log files older than this many days. Use 0 to keep logs forever." />
                        </Grid>

                        <!-- Capture transcript on close -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Capture On Close" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="CaptureOnCloseCheck" Grid.Column="1"
                                             OffContent="Save terminal transcript when pane/surface/workspace/app closes"
                                             OnContent="Save terminal transcript when pane/surface/workspace/app closes" />
                        </Grid>

                        <!-- Capture transcript on clear -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Capture On Clear" Style="{StaticResource CmuxFormLabel}" />
                            <ui:ToggleSwitch x:Name="CaptureOnClearCheck" Grid.Column="1"
                                             OffContent="Save terminal transcript before Clear Terminal"
                                             OnContent="Save terminal transcript before Clear Terminal" />
                        </Grid>

                        <!-- Transcript retention -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Capture Retention" Style="{StaticResource CmuxFormLabel}" />
                            <ui:NumberBox x:Name="TranscriptRetentionDaysBox" Grid.Column="1"
                                          Width="120" HorizontalAlignment="Left"
                                          Minimum="0" Maximum="3650" SmallChange="1" LargeChange="30"
                                          SpinButtonPlacementMode="Compact"
                                          ToolTip="Delete terminal captures older than this many days. Use 0 to keep forever." />
                        </Grid>
                    </StackPanel>

                    <!-- ═══ Keyboard Section ═══ -->
                    <StackPanel x:Name="KeyboardSection" Visibility="Collapsed">
                        <TextBlock Text="Keyboard Shortcuts" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <Border Background="#FF1A1A1A" CornerRadius="6" Padding="16,12" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                </Grid.RowDefinitions>

                                <!-- Workspaces -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Ctrl+N" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="New workspace"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Ctrl+1-8" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="Jump to workspace"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Ctrl+Shift+W" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="Close workspace"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- Surfaces -->
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Ctrl+T" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="New surface"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Ctrl+W" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="Close surface"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Ctrl+Tab" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="5" Grid.Column="1" Text="Next surface"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="Ctrl+Shift+Tab" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="6" Grid.Column="1" Text="Previous surface"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- Panes -->
                                <TextBlock Grid.Row="7" Grid.Column="0" Text="Ctrl+D" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="7" Grid.Column="1" Text="Split right"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="8" Grid.Column="0" Text="Ctrl+Shift+D" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="8" Grid.Column="1" Text="Split down"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="9" Grid.Column="0" Text="Ctrl+Alt+Arrow" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="9" Grid.Column="1" Text="Focus pane directionally"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="10" Grid.Column="0" Text="Ctrl+Shift+Z" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="10" Grid.Column="1" Text="Zoom pane toggle"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- Terminal -->
                                <TextBlock Grid.Row="11" Grid.Column="0" Text="Ctrl+Backspace" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="11" Grid.Column="1" Text="Delete previous word"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- View -->
                                <TextBlock Grid.Row="12" Grid.Column="0" Text="Ctrl+B" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="12" Grid.Column="1" Text="Toggle sidebar"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="13" Grid.Column="0" Text="Ctrl+I" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="13" Grid.Column="1" Text="Notification panel"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="14" Grid.Column="0" Text="Ctrl+Shift+U" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="14" Grid.Column="1" Text="Jump to latest unread"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="15" Grid.Column="0" Text="Ctrl+Shift+F" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="15" Grid.Column="1" Text="Search in terminal"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="16" Grid.Column="0" Text="Ctrl+Shift+P" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="16" Grid.Column="1" Text="Command palette"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="17" Grid.Column="0" Text="Ctrl+Shift+L" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="18" Grid.Column="1" Text="Open command logs"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="19" Grid.Column="0" Text="Ctrl+Alt+H" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="19" Grid.Column="1" Text="Open command history picker"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="20" Grid.Column="0" Text="Ctrl+Shift+V" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="20" Grid.Column="1" Text="Open session vault"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- ═══ About Section ═══ -->
                    <StackPanel x:Name="AboutSection" Visibility="Collapsed">
                        <TextBlock Text="About" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <Border Background="#FF1A1A1A" CornerRadius="6" Padding="20,16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="cmux for Windows" FontSize="20" FontWeight="Bold"
                                           Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,4" />
                                <TextBlock Text="v1.0.6" FontSize="13"
                                           Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12" />
                                <TextBlock Text="A modern terminal multiplexer designed for AI coding agents. Split panes, workspaces, command palette, and intelligent terminal management."
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13"
                                           TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,12" />
                                <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,4,0,12" />
                                <TextBlock Foreground="{StaticResource ForegroundDimBrush}" FontSize="12">
                                    <Run Text="Runtime: .NET 10" /><LineBreak />
                                    <Run Text="Framework: WPF + CommunityToolkit.Mvvm" /><LineBreak />
                                    <Run Text="Config: %LOCALAPPDATA%\cmux\settings.json" />
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>

            <!-- Bottom Bar -->
            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}"
                    Padding="18,12">
                <Grid>
                    <Button Content="Reset to Defaults" Style="{StaticResource CmuxButton}"
                            Click="Reset_Click" HorizontalAlignment="Left" Padding="14,7" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource CmuxButton}"
                                Click="Cancel_Click" Padding="16,7" Margin="0,0,8,0" />
                        <Button Content="Save" Style="{StaticResource AccentButton}"
                                Click="Save_Click" Padding="20,7" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
